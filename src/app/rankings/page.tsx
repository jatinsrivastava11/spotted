import { RankingsHub } from "@/components/RankingsHub";

export const metadata = {
  title: "Music Rankings & Tier Lists — Spotted",
  description: "Rank songs while listening and create custom artist tier lists",
};

export default function RankingsPage() {
  return <RankingsHub />;
}
